# 로컬 개발용. 인프라는 루트 docker-compose.yml 의 postgres 만 띄운 상태를 가정.
spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/rai_db
  jpa:
    show-sql: true

rai:
  services:
    drug-url: http://localhost:8082
    regulation-url: http://localhost:8090
