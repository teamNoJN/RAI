# RAI Frontend 환경변수
#
# 기본값은 Mock 모드(1단계 MockAiClient 전략) — 백엔드 없이 단독 실행됩니다.
# 백엔드 연동 시 아래 두 값을 .env.development 에 넣으면 코드 수정 없이 전환됩니다.

# false 로 바꾸면 실제 백엔드 호출 (기본: true)
VITE_USE_MOCK=true

# 백엔드 base URL (예: http://localhost:8090, 게이트웨이 사용 시 :8080)
VITE_API_BASE_URL=
