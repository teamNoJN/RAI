# 로컬 개발: 실백엔드 연동 (게이트웨이 경유)
# - API 는 vite 프록시(/api → http://localhost:18080)를 타므로 BASE_URL 은 비워 둔다
# - 미구현 엔드포인트(notifications, drug PATCH)는 client.ts 의 MOCK_FALLBACK 이 mock 으로 처리
VITE_USE_MOCK=false
VITE_API_BASE_URL=
